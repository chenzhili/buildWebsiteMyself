1、就是编译代码的方式，只用webpack来进行 watch，别去每次手动输入webpack，编译
2、对于其中的jquery的事件操作，用的有问题，没有用事件代理委派，事件代理委派可以监听当前绑定事件下的所有元素绑定事件，不管对dom
	进行了增加还是删除，都能绑定；
	$("document").on("事件","要操作元素的选择器",function(){});//这是普片的用法，当然document可以改成任何的父级元素